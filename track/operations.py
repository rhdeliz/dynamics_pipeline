import imagej


def track(images_to_track, image_x):

    images_list['T GFP trackmate_threshold']

    'T GFP trackmate_threshold'
    output_name
    radius_size = puncta_diameter
    noise_threshold
    trackmate_max_link_distance
    trackmate_max_gap_distance
    trackmate_frame_gap

    '/Applications/Fiji.app'

    # Something among the lines of subprocess
    from subprocess import call
    call(["ls", "-l"])

    # Since trackmate parallel processes, maybe one at a time?
